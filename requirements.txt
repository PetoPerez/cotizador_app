# Versiones FIJAS a propósito. Sin fijar, cada build de Railway instala lo más
# nuevo y el despliegue se cae con librerías que ni se tocaron:
#   - sqlalchemy 2.1.0 elige el driver psycopg (v3) por omisión y el proyecto
#     instala psycopg2-binary -> ModuleNotFoundError: No module named 'psycopg'.
#   - weasyprint 70.0 quitó `default_url_fetcher`, que usa app/services/pdf_service.py
#     -> ImportError al arrancar (comprobado en local).
# Para subir una versión: cámbiala aquí, corre las pruebas y despliega.
fastapi==0.136.1
uvicorn[standard]==0.46.0
sqlalchemy==2.0.49
psycopg2-binary==2.9.12
python-dotenv==1.2.2
bcrypt==5.0.0
python-jose[cryptography]==3.5.0
pydantic[email]==2.13.3
weasyprint==68.1
jinja2==3.1.6
python-multipart==0.0.26
pydantic-settings==2.14.0
openpyxl==3.1.5
boto3==1.42.97
slowapi==0.1.9
